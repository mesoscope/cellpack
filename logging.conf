[loggers]
keys=root,env,ingredient,grid,recipe,autopack

[handlers]
keys=consoleHandler

[formatters]
keys=simpleFormatter

[logger_root]
level=INFO
handlers=consoleHandler

[logger_autopack]
level=CRITICAL
qualname=autopack
handlers=consoleHandler

[logger_env]
level=CRITICAL
qualname=env
handlers=consoleHandler

[logger_grid]
level=INFO
qualname=grid
handlers=consoleHandler

[logger_ingredient]
level=CRITICAL
qualname=ingredient
handlers=consoleHandler

[logger_recipe]
level=CRITICAL
qualname=recipe
handlers=consoleHandler

[handler_consoleHandler]
class=StreamHandler
formatter=simpleFormatter
args=(sys.stdout,)

[formatter_simpleFormatter]
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s
datefmt=